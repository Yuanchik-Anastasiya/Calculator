<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Цвет фона -->
    <solid android:color="@color/button"/>

    <!-- Скругление углов -->
    <corners android:radius="12dp"/>

    <!-- Обводка -->
    <stroke
        android:width="5dp"
        android:color="#520000"/>

    <!-- Тень (имитация) -->
    <padding
        android:bottom="5dp"
        android:right="5dp"/>
</shape>
